{
  "name": "job-interview-todolist-fe-docs",
  "version": "1.0.0",
  "description": "Documentation for Todo List interview project",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "*.md": "prettier --write",
    "*.{json,yml,yaml}": "prettier --write"
  }
}
